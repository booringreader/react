/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{html,js,jsx}", // tailwind jurisdiction is inside the directory(src), its subdirectory(**), and all the files (with extensions,html,js)in the subdirectory
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}